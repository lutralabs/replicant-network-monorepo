query GetFundingCrowdfundings($timestamp: numeric) {
  Crowdfunding(where: {fundingPhaseEnd: {_gt: $timestamp}}) {
    id
    fundingPhaseEnd
    submissionPhaseEnd
    votingPhaseEnd
    funders {
      funder {
        id
      }
      amount
    }
    totalRaised
    numFunders
  }
}
